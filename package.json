{
  "name": "manarah",
  "private": true,
  "version": "0.1.0",
  "description": "منصة منارة لإدارة المدارس والمعاهد — Monorepo (API + Web + Desktop + Mobile)",
  "scripts": {
    "dev": "pnpm --filter @manarah/web dev",
    "build": "pnpm -r build",
    "build:web": "pnpm --filter @manarah/web build",
    "dev:api": "pnpm --filter @manarah/api start:dev",
    "test": "pnpm -r test"
  }
}
